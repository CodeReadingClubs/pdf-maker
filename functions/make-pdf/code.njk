<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8"/>
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <style>
      @import url('https://rawcdn.githack.com/googlefonts/RobotoMono/8f651634e746da6df6c2c0be73255721d24f2372/fonts/ttf/RobotoMono-Regular.ttf');
      body {
        -webkit-print-color-adjust: exact;
      }

      div {
        display: grid;
        grid-template-columns: auto 1fr;
        align-items: baseline;
        font-family: 'Roboto Mono', monospace;
        grid-column-gap: 18px;
        grid-row-gap: 6px;
      }

      .line-number {
        color: gray;
        text-align: right;
      }

      pre {
        margin: 0;
      }
    </style>
  </head>
  <body>
    <div>
      {% for line in code %}
        <span class="line-number">{{ loop.index }}</span>
        <pre>{{ line }}</pre>
      {% endfor %}
    </div>
  </body>
</html>
